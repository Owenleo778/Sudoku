import React from "react";
import {Divider, Typography} from "@material-ui/core";

const Header: React.FC = () => {
 return (
     <div>
      <Typography align="center">This is a header</Typography>
      <Divider />
     </div>
 );
}

export default Header;
